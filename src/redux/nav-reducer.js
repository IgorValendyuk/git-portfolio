let initialState = {
	friendsData: [
		{ id: 1, name: 'Vitalya' },
		{ id: 2, name: 'Valera' },
		{ id: 3, name: 'Nastya' },
	],
};

const navReducer = (state = initialState, action) => {
	return state;

}

export default navReducer;